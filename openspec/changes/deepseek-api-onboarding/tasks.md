## 1. 规范与数据边界

- [x] 1.1 定义 DeepSeek API 引导、跳过、其他 API 和连接测试的 DTO/错误契约。
- [x] 1.2 复用现有 DeepSeek 提供商预设，消除向导中重复的端点和模型常量。

## 2. 后端连接测试

- [x] 2.1 增加按 profile ID读取受保护密钥的连接测试命令，禁止前端传递明文 API Key。
- [x] 2.2 实现最小 Anthropic 兼容请求、超时、禁止重定向、响应限制和错误分类。
- [x] 2.3 增加认证失败、配置错误、网络失败、服务端失败和成功路径测试。

## 3. 前端 DeepSeek 向导

- [x] 3.1 新增分步 DeepSeek API 向导，提供申请入口、步骤进度、返回、跳过和已有其他 API 出口。
- [x] 3.2 复用原生密钥保存流程，展示基础模式字段，高级入口打开原通用配置。
- [x] 3.3 增加显式费用提醒、连接测试加载状态、成功状态和可重试错误状态。
- [x] 3.4 将新手引导默认模型步骤接入 DeepSeek 向导，成功后刷新并选中模型。
- [x] 3.5 补充键盘焦点、ARIA、浅色/夜间模式和窗口较小时的样式。

## 4. 验证与发布

- [x] 4.1 增加前端向导和 API 命令测试，覆盖取消、跳过、保存未测试、成功和失败。
+- [x] 4.2 运行前端格式、类型、Lint、测试/覆盖率和 Rust 格式、Clippy、完整测试。
+- [x] 4.3 构建 0.1.23 安装包，复制到项目根目录并推送 GitHub。
